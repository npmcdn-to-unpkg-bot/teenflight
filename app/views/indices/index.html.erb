<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
<div class="jumbotron" style="margin:0px;">
      <div class="container">
        <div style="float:right;" class="col-md-3" id="leftCol">    
          <div style="padding-left:1px;"><H4>Events</H4></div>
            <div class="well"> 
              <ul class="nav nav-stacked" id="sidebar">
                <% @indices.each do |index| %>
                  <li>
                    <%= link_to index.eventname, index %>
                  </li>
                <% end %>
              </ul>
            </div>
              <% if user_signed_in? %>
                <% if current_user.admin? %>
                  <div style="padding-left:1px;"><%= link_to 'New Event', new_index_path %></div>
                <% end %>
              <% end %>
      </div>



      <h1 style="padding-left:0px;" class="display-3">TeenFlight</h1>
        <p style="font-size:18px;">TEENFLIGHT CAMPBELL RIVER BUILDS PLANES AND ENTHUSIASM FOR AVIATION: A small group of teens, led by a smaller group of mentoring pilots, working away building a Vans RV-12. The mission of TeenFlight is to build enthusiasm for aviation, learn some skills and, above all, HAVE FUN!</p>
        <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more »</a></p>
      </div>
    </div>
  </tbody>
</table>
<br>
